#include <iostream>
int size(int a, int b)

{
  if(a>b)
  {
    return a;
  }
  else
  {
    return b;
  }
}

int main() 
{
  int num1, num2;

  printf("1번째 숫자 : ");
  scanf("%d", &num1);
   printf("2번째 숫자 : ");
  scanf("%d", &num2);

  printf("\n\n둘 중에 큰 숫자는 = %d", size(num1, num2));
}
